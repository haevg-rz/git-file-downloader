# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.idea
debug
publish
out
logs
secrets
*.key
**/test_local_dir
coverage.txt

# local clone folders for testing
clone-*
